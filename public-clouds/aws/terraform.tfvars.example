# Specify the prefix for the ec2 instance
prefix       = "suse-ai-aws"

# Specify the version of RKE2 to be installed
rke2_version = "v1.32.10+rke2r1" # Check RKE2 releases at - https://github.com/rancher/rke2/releases

# Specify the region and zone to deploy cloud resources
region = "us-west-2"
zone   = "us-west-2a"

# Specify the instance type
instance_type = "g4dn.xlarge"

# Specify the OS disk size in GB:
os_disk_size = 150

# Specify whether a new SSH key pair needs to be created for the instance
create_ssh_key_pair = true

# Specifies the full path where the pre-generated SSH PRIVATE key is located (not generated by Terraform).
ssh_private_key_path = null

# Specifies the full path where the pre-generated SSH PUBLIC key is located (not generated by Terraform).
ssh_public_key_path = null

# Specify the name of Application Registry
registry_name = "dp.apps.rancher.io"

# Specify the name of the secret for accessing the registry
registry_secretname = "application-collection"

# Specify the username and password for accessing registry
registry_username = "null"
registry_password = "null"

# Specify the namespace where you want to deploy SUSE AI
suse_ai_namespace = "suse-ai"

# Specify the namespace where you want to deploy cert-manager
cert_manager_namespace = "cert-manager"

# Specify the namespace where you want to deploy NVIDIA_GPU_OPERATOR
gpu_operator_ns = "gpu-operator"

## -- Specifies the Rancher API endpoint used to manage the SUSE AI cluster.
rancher_api_url = ""

## -- Specifies the Rancher access key for authentication.
rancher_access_key = ""

## -- Specifies the Rancher secret key for authentication.
rancher_secret_key = ""

## -- Specifies whether to allow insecure connections to the Rancher API.
rancher_insecure = false

## -- Keep it true if using existing VPC and SUBNET
use_existing_vpc = false

## -- Existing VPC ID
vpc_id = ""  # add vpc_id only if setting use_existing_vpc = true

## -- Existing SUBNET ID
subnet_id = "" # add subnet_id only if setting use_existing_vpc = true

## -- Specifies a list of CIDR blocks allowed to access port 22 (SSH).
public_ip_source_addresses = []
